---
# Run with ansible-playbook base.yml
- name: Setup Base
  hosts: all:!blogs:!pve
  remote_user: "{{ user }}"
  become: true
  become_method: sudo
  gather_facts: true

  pre_tasks:
    - name: Update Apt Cache
      ansible.builtin.apt:
        update_cache: true
        cache_valid_time: 3600

    - name: Update packages
      ansible.builtin.apt:
        name: "*"
        update_cache: true
        force_apt_get: true
        only_upgrade: true
  roles:
    - users
    - packages
    # - server
