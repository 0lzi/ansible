services:
  uptime-kuma:
    image: louislam/uptime-kuma:{{ uptime_kuma_version | default('2') }}
    container_name: uptime-kuma
    volumes:
      - {{ uptime_kuma_dir }}/data:/app/data
    ports:
      - 3001:3001
    restart: always
    labels:
      - "diun.enable=true"
