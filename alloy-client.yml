---
# Run with ansible-playbook -i inventory/hosts alloy-client.yml
- name: Setup alloy client
  hosts: vms
  remote_user: "{{ user }}"
  become: true
  become_method: sudo
  gather_facts: true

  roles:
    - users
    - alloy_client
