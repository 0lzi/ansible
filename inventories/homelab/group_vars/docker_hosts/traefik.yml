traefik_dir: "{{ base_dir }}/trafik"
cf_email: "{{ lookup('ansible.builtin.env', 'CF_EMAIL', default='na@na.com') }}"
cf_api_token: "{{ lookup('ansible.builtin.env', 'CLOUDFLARE_DNS_API_TOKEN', default='supersecure') }}"
