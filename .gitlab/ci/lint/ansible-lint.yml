---
ansible-lint:
  stage: lint
  tags:
    - ansible
  rules:
    - if: $CI_PIPELINE_SOURCE == 'merge_request_event'
  allow_failure: true
  before_script:
    - sed -i '/vault_password_file/d' ${ANSIBLE_CONFIG}
  script:
    - ansible-lint
