traefik_dir: "{{ base_dir }}/trafik"
cf_api_email: "{{ lookup('ansible.builtin.env', 'CF_API_EMAIL', default='na@na.com') }}"
cf_api_key: "{{ lookup('ansible.builtin.env', 'CF_API_KEY', default='supersecure') }}"
